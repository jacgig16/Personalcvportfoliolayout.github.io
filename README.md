npm install
npm run build

  # Personal CV Portfolio Layout

  This is a code bundle for Personal CV Portfolio Layout. The original project is available at https://www.figma.com/design/iY5gzmP0Pzhbg2dlZU5K0V/Personal-CV-Portfolio-Layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
